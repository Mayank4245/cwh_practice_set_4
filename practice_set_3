/*const prompt = require ("prompt-sync")();
// Ques.1 W.A.P to print the marks of a student in given object using for loop.

const marks = {
    harry : 98,
    rohan : 70,
    aakash : 7
}
for(let i=0;i<Object.keys(marks).length;i++){
    console.log("The marks of " + Object.keys(marks)[i] + " are " + marks[Object.keys(marks)[i]])
}
// Ques.2 Write above program in for in method.
const marks = {
    harry : 98,
    rohan : 70,
    aakash : 7
}
for (let a in marks) {
        console.log("marks of "+a+ " is "+obj[a])
}

// Ques.3 Guess the number.
let cn = 67;
while(true){
    let user_input = prompt("Enter Your Guess ");
    if(cn==user_input){
        console.log("You Guess is Correct")
        break;
    }
    else{
        if(user_input>cn){
            console.log("ohh ho.... Your Guess is Wrong Try Again You Enter Greatest Number Try Small one")
        }
        else{
            console.log("ohh ho.... Your Guess is Wrong Try Again You Enter Smallest Number Try Greater one")
        }
        console.log("ohh ho.... Your Guess is Wrong Try Again")
    }
}
// Ques.4 Make a Function Of 5 Mean Number
const mean = (p,q,r,s,t)=>{
    return (p+q+r+s+t)/5;
}
console.log("The Mean of 5 Number is ",mean(10,15,20,25,30))
*/